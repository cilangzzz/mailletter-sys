# M03 审核流程模块 - 实体设计

## 文档信息

**产品名称：** gaxx-pro 信件处理系统
**模块编号：** M03
**文档版本：** v1.0
**创建日期：** 2026-04-13
**状态：** 草稿

---

## 1. DO实体类设计

### 1.1 实体类基类说明

所有DO实体类继承 `TenantBaseDO`，包含以下公共字段：

```java
public abstract class TenantBaseDO {
    private Long tenantId;      // 租户编号
    private String creator;     // 创建者
    private LocalDateTime createTime;  // 创建时间
    private String updater;     // 更新者
    private LocalDateTime updateTime;  // 更新时间
    private Boolean deleted;    // 是否删除
}
```

### 1.2 审核记录DO (ReviewDO)

```java
package cn.iocoder.yudao.module.fz.dal.dataobject.review;

import cn.iocoder.yudao.framework.mybatis.core.dataobject.TenantBaseDO;
import com.baomidou.mybatisplus.annotation.KeySequence;
import com.baomidou.mybatisplus.annotation.TableName;
import lombok.AllArgsConstructor;
import lombok.Builder;
import lombok.Data;
import lombok.NoArgsConstructor;

import java.time.LocalDateTime;

/**
 * 审核记录 DO
 */
@TableName("fz_review")
@KeySequence("fz_review_seq")
@Data
@Builder
@NoArgsConstructor
@AllArgsConstructor
public class ReviewDO extends TenantBaseDO {

    /**
     * 主键ID
     */
    private Long id;

    /**
     * 信件ID
     */
    private Long letterId;

    /**
     * 审核编号
     */
    private String reviewNo;

    /**
     * 审核状态
     * 0-待一审、1-一审通过待终审、2-终审通过、3-审核不通过、4-已回复
     */
    private Integer reviewStatus;

    /**
     * 提交审核时间
     */
    private LocalDateTime submitTime;

    /**
     * 一审时间
     */
    private LocalDateTime firstReviewTime;

    /**
     * 一审人ID
     */
    private Long firstReviewerId;

    /**
     * 一审人姓名
     */
    private String firstReviewerName;

    /**
     * 终审时间
     */
    private LocalDateTime finalReviewTime;

    /**
     * 终审人ID
     */
    private Long finalReviewerId;

    /**
     * 终审人姓名
     */
    private String finalReviewerName;

    /**
     * 驳回次数
     */
    private Integer rejectCount;

    /**
     * 重新修改次数
     */
    private Integer modifyCount;

    /**
     * 重新办理次数
     */
    private Integer rehandleCount;

    /**
     * 回复版本号
     */
    private Integer version;
}
```

### 1.3 审核意见DO (ReviewOpinionDO)

```java
package cn.iocoder.yudao.module.fz.dal.dataobject.review;

import cn.iocoder.yudao.framework.mybatis.core.dataobject.TenantBaseDO;
import com.baomidou.mybatisplus.annotation.KeySequence;
import com.baomidou.mybatisplus.annotation.TableName;
import lombok.AllArgsConstructor;
import lombok.Builder;
import lombok.Data;
import lombok.NoArgsConstructor;

import java.time.LocalDateTime;

/**
 * 审核意见 DO
 */
@TableName("fz_review_opinion")
@KeySequence("fz_review_opinion_seq")
@Data
@Builder
@NoArgsConstructor
@AllArgsConstructor
public class ReviewOpinionDO extends TenantBaseDO {

    /**
     * 主键ID
     */
    private Long id;

    /**
     * 审核记录ID
     */
    private Long reviewId;

    /**
     * 信件ID
     */
    private Long letterId;

    /**
     * 意见类型
     * 1-一审通过、2-一审不通过、3-终审通过、4-终审不通过、5-直接终审通过
     * 6-退回重新修改、7-退回重新办理、8-下级退回、9-改派处室
     */
    private Integer opinionType;

    /**
     * 审核层级
     * 1-一审、2-终审
     */
    private Integer reviewLevel;

    /**
     * 审核意见内容
     */
    private String opinionContent;

    /**
     * 处理结果
     * 1-通过、2-不通过重新修改、3-不通过重新办理、4-退回下级
     */
    private Integer resultType;

    /**
     * 操作人ID
     */
    private Long operatorId;

    /**
     * 操作人姓名
     */
    private String operatorName;

    /**
     * 操作人单位ID
     */
    private Long operatorUnitId;

    /**
     * 操作人单位名称
     */
    private String operatorUnitName;

    /**
     * 操作角色
     * 1-接收单位、2-督办单位、3-主办单位、4-协办单位
     */
    private Integer operatorRole;

    /**
     * 操作时间
     */
    private LocalDateTime operateTime;
}
```

### 1.4 审核流程DO (ReviewFlowDO)

```java
package cn.iocoder.yudao.module.fz.dal.dataobject.review;

import cn.iocoder.yudao.framework.mybatis.core.dataobject.TenantBaseDO;
import com.baomidou.mybatisplus.annotation.KeySequence;
import com.baomidou.mybatisplus.annotation.TableName;
import lombok.AllArgsConstructor;
import lombok.Builder;
import lombok.Data;
import lombok.NoArgsConstructor;

import java.time.LocalDateTime;

/**
 * 审核流程 DO
 */
@TableName("fz_review_flow")
@KeySequence("fz_review_flow_seq")
@Data
@Builder
@NoArgsConstructor
@AllArgsConstructor
public class ReviewFlowDO extends TenantBaseDO {

    /**
     * 主键ID
     */
    private Long id;

    /**
     * 信件ID
     */
    private Long letterId;

    /**
     * 审核记录ID
     */
    private Long reviewId;

    /**
     * 流程编号
     */
    private String flowNo;

    /**
     * 流程类型
     * 1-提交回复、2-提交办理表、3-下级退回、4-上级退回重新修改、5-上级退回重新办理
     * 6-一审通过、7-终审通过、8-直接终审通过、9-审核不通过、10-改派处室、11-重新提交
     */
    private Integer flowType;

    /**
     * 流程状态
     * 0-待处理、1-处理中、2-已完成、3-已取消
     */
    private Integer flowStatus;

    /**
     * 流程名称
     */
    private String flowName;

    /**
     * 流程描述
     */
    private String flowDescription;

    /**
     * 来源节点
     */
    private String fromNode;

    /**
     * 目标节点
     */
    private String toNode;

    /**
     * 来源信件状态
     */
    private Integer fromStatus;

    /**
     * 目标信件状态
     */
    private Integer toStatus;

    /**
     * 操作人ID
     */
    private Long operatorId;

    /**
     * 操作人姓名
     */
    private String operatorName;

    /**
     * 操作人单位ID
     */
    private Long operatorUnitId;

    /**
     * 操作人单位名称
     */
    private String operatorUnitName;

    /**
     * 流程开始时间
     */
    private LocalDateTime startTime;

    /**
     * 流程结束时间
     */
    private LocalDateTime endTime;

    /**
     * 流程耗时（分钟）
     */
    private Integer durationMinutes;

    /**
     * 备注
     */
    private String remark;
}
```

### 1.5 信件回复DO (LetterReplyDO)

```java
package cn.iocoder.yudao.module.fz.dal.dataobject.review;

import cn.iocoder.yudao.framework.mybatis.core.dataobject.TenantBaseDO;
import com.baomidou.mybatisplus.annotation.KeySequence;
import com.baomidou.mybatisplus.annotation.TableName;
import lombok.AllArgsConstructor;
import lombok.Builder;
import lombok.Data;
import lombok.NoArgsConstructor;

import java.time.LocalDateTime;

/**
 * 信件回复 DO
 */
@TableName("fz_letter_reply")
@KeySequence("fz_letter_reply_seq")
@Data
@Builder
@NoArgsConstructor
@AllArgsConstructor
public class LetterReplyDO extends TenantBaseDO {

    /**
     * 主键ID
     */
    private Long id;

    /**
     * 信件ID
     */
    private Long letterId;

    /**
     * 审核记录ID
     */
    private Long reviewId;

    /**
     * 办理单位ID
     */
    private Long unitId;

    /**
     * 办理单位名称
     */
    private String unitName;

    /**
     * 单位类型
     * 1-主办单位、2-协办单位、3-督办单位
     */
    private Integer unitType;

    /**
     * 回复类型
     * 1-正式回复、2-协办意见
     */
    private Integer replyType;

    /**
     * 回复状态
     * 0-草稿、1-待审核、2-审核通过、3-审核不通过、4-已退回修改
     */
    private Integer replyStatus;

    /**
     * 回复内容
     */
    private String replyContent;

    /**
     * 回复版本号
     */
    private Integer replyVersion;

    /**
     * 是否当前版本
     */
    private Boolean isCurrent;

    /**
     * 提交时间
     */
    private LocalDateTime submitTime;

    /**
     * 审核通过时间
     */
    private LocalDateTime reviewPassTime;

    /**
     * 回复发送时间
     */
    private LocalDateTime replySendTime;

    /**
     * 驳回原因
     */
    private String rejectReason;
}
```

### 1.6 信件办理DO (LetterHandleDO)

```java
package cn.iocoder.yudao.module.fz.dal.dataobject.review;

import cn.iocoder.yudao.framework.mybatis.core.dataobject.TenantBaseDO;
import com.baomidou.mybatisplus.annotation.KeySequence;
import com.baomidou.mybatisplus.annotation.TableName;
import lombok.AllArgsConstructor;
import lombok.Builder;
import lombok.Data;
import lombok.NoArgsConstructor;

import java.time.LocalDateTime;

/**
 * 信件办理 DO
 */
@TableName("fz_letter_handle")
@KeySequence("fz_letter_handle_seq")
@Data
@Builder
@NoArgsConstructor
@AllArgsConstructor
public class LetterHandleDO extends TenantBaseDO {

    /**
     * 主键ID
     */
    private Long id;

    /**
     * 信件ID
     */
    private Long letterId;

    /**
     * 审核记录ID
     */
    private Long reviewId;

    /**
     * 办理单位ID
     */
    private Long unitId;

    /**
     * 办理单位名称
     */
    private String unitName;

    /**
     * 办理编号
     */
    private String handleNo;

    /**
     * 办理状态
     * 0-待办理、1-办理中、2-已提交、3-已退回
     */
    private Integer handleStatus;

    /**
     * 办理版本号
     */
    private Integer handleVersion;

    /**
     * 是否当前版本
     */
    private Boolean isCurrent;

    // 办理人信息
    /**
     * 办理人姓名
     */
    private String handlerName;

    /**
     * 办理人身份证号（加密）
     */
    private String handlerIdCard;

    /**
     * 办理人联系电话
     */
    private String handlerPhone;

    /**
     * 办理人所在单位
     */
    private String handlerUnit;

    // 回访信息
    /**
     * 是否已回访
     */
    private Boolean isRevisited;

    /**
     * 回访时间
     */
    private LocalDateTime visitTime;

    /**
     * 回访情况
     */
    private String visitSituation;

    /**
     * 群众反映
     */
    private String publicReflect;

    /**
     * 未回访原因
     */
    private String noVisitReason;

    // 接访信息
    /**
     * 是否已接访
     */
    private Boolean isReceived;

    /**
     * 接访领导
     */
    private String receiveLeader;

    /**
     * 接访时间
     */
    private LocalDateTime receiveTime;

    /**
     * 接访方式
     * 1-现场接访、2-电话接访、3-视频接访
     */
    private Integer receiveType;

    /**
     * 接访情况
     */
    private String receiveSituation;

    // 调查信息
    /**
     * 调查情况
     */
    private String investigationSituation;

    /**
     * 无法调查原因
     */
    private String noInvestigationReason;

    // 满意度评价
    /**
     * 诉求解决满意度（1-5分）
     */
    private Integer satisfactionResolve;

    /**
     * 响应速度满意度（1-5分）
     */
    private Integer satisfactionResponse;

    /**
     * 办理态度满意度（1-5分）
     */
    private Integer satisfactionAttitude;

    /**
     * 整体满意度（1-5分）
     */
    private Integer satisfactionOverall;

    /**
     * 满意度备注
     */
    private String satisfactionRemark;

    // 提交信息
    /**
     * 提交时间
     */
    private LocalDateTime submitTime;

    /**
     * 提交人ID
     */
    private Long submitterId;

    /**
     * 提交人姓名
     */
    private String submitterName;
}
```

---

## 2. VO类设计

### 2.1 请求VO分类

| VO类型 | 用途 | 命名规则 |
|--------|------|----------|
| SaveReqVO | 新增/修改请求 | XxxSaveReqVO |
| SubmitReqVO | 提交操作请求 | XxxSubmitReqVO |
| PageReqVO | 分页查询请求 | XxxPageReqVO |
| ActionReqVO | 操作动作请求 | XxxActionReqVO |

### 2.2 响应VO分类

| VO类型 | 用途 | 命名规则 |
|--------|------|----------|
| RespVO | 单实体响应 | XxxRespVO |
| DetailRespVO | 详情响应 | XxxDetailRespVO |
| ListRespVO | 列表响应 | XxxListRespVO |

### 2.3 提交回复请求VO (ReplySubmitReqVO)

```java
package cn.iocoder.yudao.module.fz.controller.admin.review.vo;

import io.swagger.v3.oas.annotations.media.Schema;
import lombok.Data;
import javax.validation.constraints.NotBlank;
import javax.validation.constraints.NotNull;
import javax.validation.constraints.Size;

@Schema(description = "提交回复请求VO")
@Data
public class ReplySubmitReqVO {

    @Schema(description = "信件ID", requiredMode = Schema.RequiredMode.REQUIRED, example = "1")
    @NotNull(message = "信件ID不能为空")
    private Long letterId;

    @Schema(description = "回复内容", requiredMode = Schema.RequiredMode.REQUIRED, example = "您好，关于您反映的问题...")
    @NotBlank(message = "回复内容不能为空")
    @Size(max = 5000, message = "回复内容长度不能超过5000字符")
    private String replyContent;

    @Schema(description = "是否同时提交办理表", example = "true")
    private Boolean submitHandleTogether;

    @Schema(description = "办理表信息（同时提交时必填）")
    private HandleInfoVO handleInfo;
}
```

### 2.4 保存回复草稿请求VO (ReplyDraftSaveReqVO)

```java
package cn.iocoder.yudao.module.fz.controller.admin.review.vo;

import io.swagger.v3.oas.annotations.media.Schema;
import lombok.Data;
import javax.validation.constraints.NotNull;
import javax.validation.constraints.Size;

@Schema(description = "保存回复草稿请求VO")
@Data
public class ReplyDraftSaveReqVO {

    @Schema(description = "信件ID", requiredMode = Schema.RequiredMode.REQUIRED, example = "1")
    @NotNull(message = "信件ID不能为空")
    private Long letterId;

    @Schema(description = "回复内容", example = "您好，关于您反映的问题...")
    @Size(max = 5000, message = "回复内容长度不能超过5000字符")
    private String replyContent;

    @Schema(description = "草稿ID（更新草稿时传入）", example = "1")
    private Long draftId;
}
```

### 2.5 提交办理表请求VO (HandleSubmitReqVO)

```java
package cn.iocoder.yudao.module.fz.controller.admin.review.vo;

import io.swagger.v3.oas.annotations.media.Schema;
import lombok.Data;
import javax.validation.constraints.*;
import java.time.LocalDateTime;

@Schema(description = "提交办理表请求VO")
@Data
public class HandleSubmitReqVO {

    @Schema(description = "信件ID", requiredMode = Schema.RequiredMode.REQUIRED, example = "1")
    @NotNull(message = "信件ID不能为空")
    private Long letterId;

    // 办理人信息
    @Schema(description = "办理人姓名", requiredMode = Schema.RequiredMode.REQUIRED, example = "张三")
    @NotBlank(message = "办理人姓名不能为空")
    private String handlerName;

    @Schema(description = "办理人身份证号", example = "123456789012345678")
    @Pattern(regexp = "^[1-9]\\d{5}(18|19|20)\\d{2}(0[1-9]|1[0-2])(0[1-9]|[12]\\d|3[01])\\d{3}[0-9Xx]$",
             message = "身份证号格式不正确")
    private String handlerIdCard;

    @Schema(description = "办理人联系电话", example = "13800138000")
    private String handlerPhone;

    @Schema(description = "办理人所在单位", example = "XX分局")
    private String handlerUnit;

    // 回访信息
    @Schema(description = "是否已回访", example = "true")
    private Boolean isRevisited;

    @Schema(description = "回访时间")
    private LocalDateTime visitTime;

    @Schema(description = "回访情况", example = "已电话回访，群众表示满意")
    @Size(max = 1000, message = "回访情况长度不能超过1000字符")
    private String visitSituation;

    @Schema(description = "群众反映", example = "群众对处理结果表示认可")
    @Size(max = 1000, message = "群众反映长度不能超过1000字符")
    private String publicReflect;

    @Schema(description = "未回访原因（未回访时必填）", example = "群众电话无法接通")
    @Size(max = 500, message = "未回访原因长度不能超过500字符")
    private String noVisitReason;

    // 接访信息
    @Schema(description = "是否已接访", example = "true")
    private Boolean isReceived;

    @Schema(description = "接访领导", example = "王局长")
    private String receiveLeader;

    @Schema(description = "接访时间")
    private LocalDateTime receiveTime;

    @Schema(description = "接访方式：1-现场接访、2-电话接访、3-视频接访", example = "1")
    private Integer receiveType;

    @Schema(description = "接访情况")
    @Size(max = 1000, message = "接访情况长度不能超过1000字符")
    private String receiveSituation;

    // 调查信息
    @Schema(description = "调查情况")
    @Size(max = 2000, message = "调查情况长度不能超过2000字符")
    private String investigationSituation;

    @Schema(description = "无法调查原因")
    @Size(max = 500, message = "无法调查原因长度不能超过500字符")
    private String noInvestigationReason;

    // 满意度评价
    @Schema(description = "诉求解决满意度（1-5分）", example = "5")
    @Min(value = 1, message = "满意度评分最小为1")
    @Max(value = 5, message = "满意度评分最大为5")
    private Integer satisfactionResolve;

    @Schema(description = "响应速度满意度（1-5分）", example = "4")
    @Min(value = 1, message = "满意度评分最小为1")
    @Max(value = 5, message = "满意度评分最大为5")
    private Integer satisfactionResponse;

    @Schema(description = "办理态度满意度（1-5分）", example = "5")
    @Min(value = 1, message = "满意度评分最小为1")
    @Max(value = 5, message = "满意度评分最大为5")
    private Integer satisfactionAttitude;

    @Schema(description = "整体满意度（1-5分）", example = "4")
    @Min(value = 1, message = "满意度评分最小为1")
    @Max(value = 5, message = "满意度评分最大为5")
    private Integer satisfactionOverall;

    @Schema(description = "满意度备注")
    @Size(max = 500, message = "满意度备注长度不能超过500字符")
    private String satisfactionRemark;
}
```

### 2.6 下级退回请求VO (ReturnDownReqVO)

```java
package cn.iocoder.yudao.module.fz.controller.admin.review.vo;

import io.swagger.v3.oas.annotations.media.Schema;
import lombok.Data;
import javax.validation.constraints.NotBlank;
import javax.validation.constraints.NotNull;
import javax.validation.constraints.Size;

@Schema(description = "下级退回请求VO")
@Data
public class ReturnDownReqVO {

    @Schema(description = "信件ID", requiredMode = Schema.RequiredMode.REQUIRED, example = "1")
    @NotNull(message = "信件ID不能为空")
    private Long letterId;

    @Schema(description = "退回原因", requiredMode = Schema.RequiredMode.REQUIRED, example = "本单位职责范围外，无法处理")
    @NotBlank(message = "退回原因不能为空")
    @Size(min = 10, max = 500, message = "退回原因长度需在10-500字符之间")
    private String returnReason;
}
```

### 2.7 上级退回重新修改请求VO (ReturnModifyReqVO)

```java
package cn.iocoder.yudao.module.fz.controller.admin.review.vo;

import io.swagger.v3.oas.annotations.media.Schema;
import lombok.Data;
import javax.validation.constraints.NotBlank;
import javax.validation.constraints.NotNull;
import javax.validation.constraints.Size;

@Schema(description = "上级退回重新修改请求VO")
@Data
public class ReturnModifyReqVO {

    @Schema(description = "信件ID", requiredMode = Schema.RequiredMode.REQUIRED, example = "1")
    @NotNull(message = "信件ID不能为空")
    private Long letterId;

    @Schema(description = "审核记录ID", requiredMode = Schema.RequiredMode.REQUIRED, example = "1")
    @NotNull(message = "审核记录ID不能为空")
    private Long reviewId;

    @Schema(description = "退回意见", requiredMode = Schema.RequiredMode.REQUIRED, example = "回复内容表述不清，请补充具体处理措施")
    @NotBlank(message = "退回意见不能为空")
    @Size(min = 10, max = 1000, message = "退回意见长度需在10-1000字符之间")
    private String returnOpinion;
}
```

### 2.8 上级退回重新办理请求VO (ReturnRehandleReqVO)

```java
package cn.iocoder.yudao.module.fz.controller.admin.review.vo;

import io.swagger.v3.oas.annotations.media.Schema;
import lombok.Data;
import javax.validation.constraints.NotBlank;
import javax.validation.constraints.NotNull;
import javax.validation.constraints.Size;

@Schema(description = "上级退回重新办理请求VO")
@Data
public class ReturnRehandleReqVO {

    @Schema(description = "信件ID", requiredMode = Schema.RequiredMode.REQUIRED, example = "1")
    @NotNull(message = "信件ID不能为空")
    private Long letterId;

    @Schema(description = "审核记录ID", requiredMode = Schema.RequiredMode.REQUIRED, example = "1")
    @NotNull(message = "审核记录ID不能为空")
    private Long reviewId;

    @Schema(description = "退回意见", requiredMode = Schema.RequiredMode.REQUIRED, example = "调查情况不详细，请重新调查核实")
    @NotBlank(message = "退回意见不能为空")
    @Size(min = 10, max = 1000, message = "退回意见长度需在10-1000字符之间")
    private String returnOpinion;
}
```

### 2.9 一审通过请求VO (FirstPassReqVO)

```java
package cn.iocoder.yudao.module.fz.controller.admin.review.vo;

import io.swagger.v3.oas.annotations.media.Schema;
import lombok.Data;
import javax.validation.constraints.NotNull;
import javax.validation.constraints.Size;

@Schema(description = "一审通过请求VO")
@Data
public class FirstPassReqVO {

    @Schema(description = "信件ID", requiredMode = Schema.RequiredMode.REQUIRED, example = "1")
    @NotNull(message = "信件ID不能为空")
    private Long letterId;

    @Schema(description = "审核记录ID", requiredMode = Schema.RequiredMode.REQUIRED, example = "1")
    @NotNull(message = "审核记录ID不能为空")
    private Long reviewId;

    @Schema(description = "审核意见", example = "回复内容规范，事实清楚，同意通过")
    @Size(max = 1000, message = "审核意见长度不能超过1000字符")
    private String reviewOpinion;
}
```

### 2.10 终审通过请求VO (FinalPassReqVO)

```java
package cn.iocoder.yudao.module.fz.controller.admin.review.vo;

import io.swagger.v3.oas.annotations.media.Schema;
import lombok.Data;
import javax.validation.constraints.NotNull;
import javax.validation.constraints.Size;

@Schema(description = "终审通过请求VO")
@Data
public class FinalPassReqVO {

    @Schema(description = "信件ID", requiredMode = Schema.RequiredMode.REQUIRED, example = "1")
    @NotNull(message = "信件ID不能为空")
    private Long letterId;

    @Schema(description = "审核记录ID", requiredMode = Schema.RequiredMode.REQUIRED, example = "1")
    @NotNull(message = "审核记录ID不能为空")
    private Long reviewId;

    @Schema(description = "审核意见", example = "终审通过，回复内容符合规范要求")
    @Size(max = 1000, message = "审核意见长度不能超过1000字符")
    private String reviewOpinion;
}
```

### 2.11 直接终审通过请求VO (DirectFinalPassReqVO)

```java
package cn.iocoder.yudao.module.fz.controller.admin.review.vo;

import io.swagger.v3.oas.annotations.media.Schema;
import lombok.Data;
import javax.validation.constraints.NotNull;
import javax.validation.constraints.Size;

@Schema(description = "直接终审通过请求VO")
@Data
public class DirectFinalPassReqVO {

    @Schema(description = "信件ID", requiredMode = Schema.RequiredMode.REQUIRED, example = "1")
    @NotNull(message = "信件ID不能为空")
    private Long letterId;

    @Schema(description = "审核记录ID", requiredMode = Schema.RequiredMode.REQUIRED, example = "1")
    @NotNull(message = "审核记录ID不能为空")
    private Long reviewId;

    @Schema(description = "审核意见", example = "紧急事项，直接终审通过")
    @Size(max = 1000, message = "审核意见长度不能超过1000字符")
    private String reviewOpinion;

    @Schema(description = "直接终审原因", example = "紧急信件处理")
    @Size(max = 500, message = "直接终审原因长度不能超过500字符")
    private String directReason;
}
```

### 2.12 审核不通过请求VO (RejectReqVO)

```java
package cn.iocoder.yudao.module.fz.controller.admin.review.vo;

import io.swagger.v3.oas.annotations.media.Schema;
import lombok.Data;
import javax.validation.constraints.NotBlank;
import javax.validation.constraints.NotNull;
import javax.validation.constraints.Size;

@Schema(description = "审核不通过请求VO")
@Data
public class RejectReqVO {

    @Schema(description = "信件ID", requiredMode = Schema.RequiredMode.REQUIRED, example = "1")
    @NotNull(message = "信件ID不能为空")
    private Long letterId;

    @Schema(description = "审核记录ID", requiredMode = Schema.RequiredMode.REQUIRED, example = "1")
    @NotNull(message = "审核记录ID不能为空")
    private Long reviewId;

    @Schema(description = "驳回原因", requiredMode = Schema.RequiredMode.REQUIRED, example = "回复内容与事实不符")
    @NotBlank(message = "驳回原因不能为空")
    @Size(min = 10, max = 1000, message = "驳回原因长度需在10-1000字符之间")
    private String rejectReason;

    @Schema(description = "后续处理方式：1-重新修改、2-重新办理", requiredMode = Schema.RequiredMode.REQUIRED, example = "1")
    @NotNull(message = "后续处理方式不能为空")
    private Integer handleType;
}
```

### 2.13 改派处室请求VO (ReassignUnitReqVO)

```java
package cn.iocoder.yudao.module.fz.controller.admin.review.vo;

import io.swagger.v3.oas.annotations.media.Schema;
import lombok.Data;
import javax.validation.constraints.NotBlank;
import javax.validation.constraints.NotNull;
import javax.validation.constraints.Size;

@Schema(description = "改派处室请求VO")
@Data
public class ReassignUnitReqVO {

    @Schema(description = "信件ID", requiredMode = Schema.RequiredMode.REQUIRED, example = "1")
    @NotNull(message = "信件ID不能为空")
    private Long letterId;

    @Schema(description = "改派类型：1-改派主办单位、2-增加协办单位、3-取消协办单位", requiredMode = Schema.RequiredMode.REQUIRED, example = "1")
    @NotNull(message = "改派类型不能为空")
    private Integer reassignType;

    @Schema(description = "原单位ID（改派或取消时必填）", example = "1")
    private Long originalUnitId;

    @Schema(description = "新单位ID（改派或增加时必填）", example = "2")
    private Long newUnitId;

    @Schema(description = "改派原因", requiredMode = Schema.RequiredMode.REQUIRED, example = "原单位职责调整，需改派其他单位")
    @NotBlank(message = "改派原因不能为空")
    @Size(min = 10, max = 500, message = "改派原因长度需在10-500字符之间")
    private String reassignReason;
}
```

### 2.14 审核流程响应VO (ReviewFlowRespVO)

```java
package cn.iocoder.yudao.module.fz.controller.admin.review.vo;

import io.swagger.v3.oas.annotations.media.Schema;
import lombok.Data;
import java.time.LocalDateTime;
import java.util.List;

@Schema(description = "审核流程响应VO")
@Data
public class ReviewFlowRespVO {

    @Schema(description = "信件ID")
    private Long letterId;

    @Schema(description = "审核记录ID")
    private Long reviewId;

    @Schema(description = "审核编号")
    private String reviewNo;

    @Schema(description = "审核状态")
    private Integer reviewStatus;

    @Schema(description = "审核状态名称")
    private String reviewStatusName;

    @Schema(description = "当前流程节点")
    private String currentNode;

    @Schema(description = "当前处理角色")
    private String currentRole;

    @Schema(description = "提交时间")
    private LocalDateTime submitTime;

    @Schema(description = "一审时间")
    private LocalDateTime firstReviewTime;

    @Schema(description = "终审时间")
    private LocalDateTime finalReviewTime;

    @Schema(description = "驳回次数")
    private Integer rejectCount;

    @Schema(description = "重新修改次数")
    private Integer modifyCount;

    @Schema(description = "重新办理次数")
    private Integer rehandleCount;

    @Schema(description = "回复版本号")
    private Integer version;

    @Schema(description = "流程节点列表")
    private List<FlowNodeVO> flowNodes;
}
```

### 2.15 流程节点VO (FlowNodeVO)

```java
package cn.iocoder.yudao.module.fz.controller.admin.review.vo;

import io.swagger.v3.oas.annotations.media.Schema;
import lombok.Data;
import java.time.LocalDateTime;

@Schema(description = "流程节点VO")
@Data
public class FlowNodeVO {

    @Schema(description = "流程记录ID")
    private Long flowId;

    @Schema(description = "流程编号")
    private String flowNo;

    @Schema(description = "流程类型")
    private Integer flowType;

    @Schema(description = "流程类型名称")
    private String flowTypeName;

    @Schema(description = "流程名称")
    private String flowName;

    @Schema(description = "流程描述")
    private String flowDescription;

    @Schema(description = "来源信件状态")
    private Integer fromStatus;

    @Schema(description = "目标信件状态")
    private Integer toStatus;

    @Schema(description = "操作人姓名")
    private String operatorName;

    @Schema(description = "操作人单位名称")
    private String operatorUnitName;

    @Schema(description = "流程开始时间")
    private LocalDateTime startTime;

    @Schema(description = "流程结束时间")
    private LocalDateTime endTime;

    @Schema(description = "流程耗时（分钟）")
    private Integer durationMinutes;

    @Schema(description = "备注")
    private String remark;
}
```

### 2.16 审核详情响应VO (ReviewDetailRespVO)

```java
package cn.iocoder.yudao.module.fz.controller.admin.review.vo;

import io.swagger.v3.oas.annotations.media.Schema;
import lombok.Data;
import java.time.LocalDateTime;
import java.util.List;

@Schema(description = "审核详情响应VO")
@Data
public class ReviewDetailRespVO {

    @Schema(description = "审核记录ID")
    private Long id;

    @Schema(description = "审核编号")
    private String reviewNo;

    @Schema(description = "信件ID")
    private Long letterId;

    @Schema(description = "审核状态")
    private Integer reviewStatus;

    @Schema(description = "审核状态名称")
    private String reviewStatusName;

    @Schema(description = "提交审核时间")
    private LocalDateTime submitTime;

    @Schema(description = "一审时间")
    private LocalDateTime firstReviewTime;

    @Schema(description = "一审人姓名")
    private String firstReviewerName;

    @Schema(description = "终审时间")
    private LocalDateTime finalReviewTime;

    @Schema(description = "终审人姓名")
    private String finalReviewerName;

    @Schema(description = "驳回次数")
    private Integer rejectCount;

    @Schema(description = "重新修改次数")
    private Integer modifyCount;

    @Schema(description = "重新办理次数")
    private Integer rehandleCount;

    @Schema(description = "回复版本号")
    private Integer version;

    @Schema(description = "审核意见列表")
    private List<ReviewOpinionVO> opinions;

    @Schema(description = "回复内容")
    private String replyContent;

    @Schema(description = "办理表信息")
    private HandleInfoVO handleInfo;
}
```

### 2.17 审核意见VO (ReviewOpinionVO)

```java
package cn.iocoder.yudao.module.fz.controller.admin.review.vo;

import io.swagger.v3.oas.annotations.media.Schema;
import lombok.Data;
import java.time.LocalDateTime;

@Schema(description = "审核意见VO")
@Data
public class ReviewOpinionVO {

    @Schema(description = "意见记录ID")
    private Long id;

    @Schema(description = "意见类型")
    private Integer opinionType;

    @Schema(description = "意见类型名称")
    private String opinionTypeName;

    @Schema(description = "审核层级")
    private Integer reviewLevel;

    @Schema(description = "审核意见内容")
    private String opinionContent;

    @Schema(description = "处理结果")
    private Integer resultType;

    @Schema(description = "处理结果名称")
    private String resultTypeName;

    @Schema(description = "操作人姓名")
    private String operatorName;

    @Schema(description = "操作人单位名称")
    private String operatorUnitName;

    @Schema(description = "操作角色")
    private Integer operatorRole;

    @Schema(description = "操作角色名称")
    private String operatorRoleName;

    @Schema(description = "操作时间")
    private LocalDateTime operateTime;
}
```

### 2.18 办理表信息VO (HandleInfoVO)

```java
package cn.iocoder.yudao.module.fz.controller.admin.review.vo;

import io.swagger.v3.oas.annotations.media.Schema;
import lombok.Data;
import java.time.LocalDateTime;

@Schema(description = "办理表信息VO")
@Data
public class HandleInfoVO {

    @Schema(description = "办理记录ID")
    private Long id;

    @Schema(description = "办理编号")
    private String handleNo;

    @Schema(description = "办理状态")
    private Integer handleStatus;

    @Schema(description = "办理状态名称")
    private String handleStatusName;

    // 办理人信息
    @Schema(description = "办理人姓名")
    private String handlerName;

    @Schema(description = "办理人联系电话")
    private String handlerPhone;

    @Schema(description = "办理人所在单位")
    private String handlerUnit;

    // 回访信息
    @Schema(description = "是否已回访")
    private Boolean isRevisited;

    @Schema(description = "回访时间")
    private LocalDateTime visitTime;

    @Schema(description = "回访情况")
    private String visitSituation;

    @Schema(description = "群众反映")
    private String publicReflect;

    @Schema(description = "未回访原因")
    private String noVisitReason;

    // 接访信息
    @Schema(description = "是否已接访")
    private Boolean isReceived;

    @Schema(description = "接访领导")
    private String receiveLeader;

    @Schema(description = "接访时间")
    private LocalDateTime receiveTime;

    @Schema(description = "接访方式")
    private Integer receiveType;

    @Schema(description = "接访方式名称")
    private String receiveTypeName;

    @Schema(description = "接访情况")
    private String receiveSituation;

    // 调查信息
    @Schema(description = "调查情况")
    private String investigationSituation;

    @Schema(description = "无法调查原因")
    private String noInvestigationReason;

    // 满意度评价
    @Schema(description = "诉求解决满意度")
    private Integer satisfactionResolve;

    @Schema(description = "响应速度满意度")
    private Integer satisfactionResponse;

    @Schema(description = "办理态度满意度")
    private Integer satisfactionAttitude;

    @Schema(description = "整体满意度")
    private Integer satisfactionOverall;

    @Schema(description = "满意度备注")
    private String satisfactionRemark;

    // 提交信息
    @Schema(description = "提交时间")
    private LocalDateTime submitTime;

    @Schema(description = "提交人姓名")
    private String submitterName;
}
```

---

## 3. 枚举类设计

### 3.1 审核状态枚举 (ReviewStatusEnum)

```java
package cn.iocoder.yudao.module.fz.enums.review;

import cn.iocoder.yudao.framework.common.core.IntArrayValuable;
import lombok.AllArgsConstructor;
import lombok.Getter;

import java.util.Arrays;

/**
 * 审核状态枚举
 */
@Getter
@AllArgsConstructor
public enum ReviewStatusEnum implements IntArrayValuable {

    WAITING_FIRST(0, "待一审"),
    FIRST_PASSED(1, "一审通过待终审"),
    FINAL_PASSED(2, "终审通过"),
    REJECTED(3, "审核不通过"),
    REPLIED(4, "已回复");

    public static final int[] ARRAYS = Arrays.stream(values()).mapToInt(ReviewStatusEnum::getStatus).toArray();

    /**
     * 状态值
     */
    private final Integer status;

    /**
     * 状态名称
     */
    private final String name;

    @Override
    public int[] array() {
        return ARRAYS;
    }

    public static ReviewStatusEnum valueOf(Integer status) {
        return Arrays.stream(values())
                .filter(e -> e.getStatus().equals(status))
                .findFirst()
                .orElse(null);
    }

    public static String getName(Integer status) {
        ReviewStatusEnum enumObj = valueOf(status);
        return enumObj != null ? enumObj.getName() : "";
    }
}
```

### 3.2 审核意见类型枚举 (OpinionTypeEnum)

```java
package cn.iocoder.yudao.module.fz.enums.review;

import cn.iocoder.yudao.framework.common.core.IntArrayValuable;
import lombok.AllArgsConstructor;
import lombok.Getter;

import java.util.Arrays;

/**
 * 审核意见类型枚举
 */
@Getter
@AllArgsConstructor
public enum OpinionTypeEnum implements IntArrayValuable {

    FIRST_PASS(1, "一审通过"),
    FIRST_REJECT(2, "一审不通过"),
    FINAL_PASS(3, "终审通过"),
    FINAL_REJECT(4, "终审不通过"),
    DIRECT_FINAL_PASS(5, "直接终审通过"),
    RETURN_MODIFY(6, "退回重新修改"),
    RETURN_REHANDLE(7, "退回重新办理"),
    RETURN_DOWN(8, "下级退回"),
    REASSIGN_UNIT(9, "改派处室");

    public static final int[] ARRAYS = Arrays.stream(values()).mapToInt(OpinionTypeEnum::getType).toArray();

    /**
     * 类型值
     */
    private final Integer type;

    /**
     * 类型名称
     */
    private final String name;

    @Override
    public int[] array() {
        return ARRAYS;
    }

    public static OpinionTypeEnum valueOf(Integer type) {
        return Arrays.stream(values())
                .filter(e -> e.getType().equals(type))
                .findFirst()
                .orElse(null);
    }

    public static String getName(Integer type) {
        OpinionTypeEnum enumObj = valueOf(type);
        return enumObj != null ? enumObj.getName() : "";
    }
}
```

### 3.3 流程类型枚举 (FlowTypeEnum)

```java
package cn.iocoder.yudao.module.fz.enums.review;

import cn.iocoder.yudao.framework.common.core.IntArrayValuable;
import lombok.AllArgsConstructor;
import lombok.Getter;

import java.util.Arrays;

/**
 * 流程类型枚举
 */
@Getter
@AllArgsConstructor
public enum FlowTypeEnum implements IntArrayValuable {

    SUBMIT_REPLY(1, "提交回复"),
    SUBMIT_HANDLE(2, "提交办理表"),
    RETURN_DOWN(3, "下级退回"),
    RETURN_MODIFY(4, "上级退回重新修改"),
    RETURN_REHANDLE(5, "上级退回重新办理"),
    FIRST_PASS(6, "一审通过"),
    FINAL_PASS(7, "终审通过"),
    DIRECT_FINAL_PASS(8, "直接终审通过"),
    REVIEW_REJECT(9, "审核不通过"),
    REASSIGN_UNIT(10, "改派处室"),
    RESUBMIT(11, "重新提交");

    public static final int[] ARRAYS = Arrays.stream(values()).mapToInt(FlowTypeEnum::getType).toArray();

    /**
     * 类型值
     */
    private final Integer type;

    /**
     * 类型名称
     */
    private final String name;

    @Override
    public int[] array() {
        return ARRAYS;
    }

    public static FlowTypeEnum valueOf(Integer type) {
        return Arrays.stream(values())
                .filter(e -> e.getType().equals(type))
                .findFirst()
                .orElse(null);
    }

    public static String getName(Integer type) {
        FlowTypeEnum enumObj = valueOf(type);
        return enumObj != null ? enumObj.getName() : "";
    }
}
```

### 3.4 回复状态枚举 (ReplyStatusEnum)

```java
package cn.iocoder.yudao.module.fz.enums.review;

import cn.iocoder.yudao.framework.common.core.IntArrayValuable;
import lombok.AllArgsConstructor;
import lombok.Getter;

import java.util.Arrays;

/**
 * 回复状态枚举
 */
@Getter
@AllArgsConstructor
public enum ReplyStatusEnum implements IntArrayValuable {

    DRAFT(0, "草稿"),
    WAITING_REVIEW(1, "待审核"),
    REVIEW_PASSED(2, "审核通过"),
    REVIEW_REJECTED(3, "审核不通过"),
    RETURNED_MODIFY(4, "已退回修改");

    public static final int[] ARRAYS = Arrays.stream(values()).mapToInt(ReplyStatusEnum::getStatus).toArray();

    /**
     * 状态值
     */
    private final Integer status;

    /**
     * 状态名称
     */
    private final String name;

    @Override
    public int[] array() {
        return ARRAYS;
    }

    public static ReplyStatusEnum valueOf(Integer status) {
        return Arrays.stream(values())
                .filter(e -> e.getStatus().equals(status))
                .findFirst()
                .orElse(null);
    }

    public static String getName(Integer status) {
        ReplyStatusEnum enumObj = valueOf(status);
        return enumObj != null ? enumObj.getName() : "";
    }
}
```

### 3.5 办理状态枚举 (HandleStatusEnum)

```java
package cn.iocoder.yudao.module.fz.enums.review;

import cn.iocoder.yudao.framework.common.core.IntArrayValuable;
import lombok.AllArgsConstructor;
import lombok.Getter;

import java.util.Arrays;

/**
 * 办理状态枚举
 */
@Getter
@AllArgsConstructor
public enum HandleStatusEnum implements IntArrayValuable {

    WAITING(0, "待办理"),
    HANDLING(1, "办理中"),
    SUBMITTED(2, "已提交"),
    RETURNED(3, "已退回");

    public static final int[] ARRAYS = Arrays.stream(values()).mapToInt(HandleStatusEnum::getStatus).toArray();

    /**
     * 状态值
     */
    private final Integer status;

    /**
     * 状态名称
     */
    private final String name;

    @Override
    public int[] array() {
        return ARRAYS;
    }

    public static HandleStatusEnum valueOf(Integer status) {
        return Arrays.stream(values())
                .filter(e -> e.getStatus().equals(status))
                .findFirst()
                .orElse(null);
    }

    public static String getName(Integer status) {
        HandleStatusEnum enumObj = valueOf(status);
        return enumObj != null ? enumObj.getName() : "";
    }
}
```

### 3.6 操作角色枚举 (OperatorRoleEnum)

```java
package cn.iocoder.yudao.module.fz.enums.review;

import cn.iocoder.yudao.framework.common.core.IntArrayValuable;
import lombok.AllArgsConstructor;
import lombok.Getter;

import java.util.Arrays;

/**
 * 操作角色枚举
 */
@Getter
@AllArgsConstructor
public enum OperatorRoleEnum implements IntArrayValuable {

    RECEIVE_UNIT(1, "接收单位"),
    SUPERVISION_UNIT(2, "督办单位"),
    HOST_UNIT(3, "主办单位"),
    ASSIST_UNIT(4, "协办单位");

    public static final int[] ARRAYS = Arrays.stream(values()).mapToInt(OperatorRoleEnum::getRole).toArray();

    /**
     * 角色值
     */
    private final Integer role;

    /**
     * 角色名称
     */
    private final String name;

    @Override
    public int[] array() {
        return ARRAYS;
    }

    public static OperatorRoleEnum valueOf(Integer role) {
        return Arrays.stream(values())
                .filter(e -> e.getRole().equals(role))
                .findFirst()
                .orElse(null);
    }

    public static String getName(Integer role) {
        OperatorRoleEnum enumObj = valueOf(role);
        return enumObj != null ? enumObj.getName() : "";
    }
}
```

### 3.7 处理结果枚举 (ResultTypeEnum)

```java
package cn.iocoder.yudao.module.fz.enums.review;

import cn.iocoder.yudao.framework.common.core.IntArrayValuable;
import lombok.AllArgsConstructor;
import lombok.Getter;

import java.util.Arrays;

/**
 * 处理结果枚举
 */
@Getter
@AllArgsConstructor
public enum ResultTypeEnum implements IntArrayValuable {

    PASS(1, "通过"),
    REJECT_MODIFY(2, "不通过重新修改"),
    REJECT_REHANDLE(3, "不通过重新办理"),
    RETURN_DOWN(4, "退回下级");

    public static final int[] ARRAYS = Arrays.stream(values()).mapToInt(ResultTypeEnum::getType).toArray();

    /**
     * 类型值
     */
    private final Integer type;

    /**
     * 类型名称
     */
    private final String name;

    @Override
    public int[] array() {
        return ARRAYS;
    }

    public static ResultTypeEnum valueOf(Integer type) {
        return Arrays.stream(values())
                .filter(e -> e.getType().equals(type))
                .findFirst()
                .orElse(null);
    }

    public static String getName(Integer type) {
        ResultTypeEnum enumObj = valueOf(type);
        return enumObj != null ? enumObj.getName() : "";
    }
}
```

### 3.8 改派类型枚举 (ReassignTypeEnum)

```java
package cn.iocoder.yudao.module.fz.enums.review;

import cn.iocoder.yudao.framework.common.core.IntArrayValuable;
import lombok.AllArgsConstructor;
import lombok.Getter;

import java.util.Arrays;

/**
 * 改派类型枚举
 */
@Getter
@AllArgsConstructor
public enum ReassignTypeEnum implements IntArrayValuable {

    REASSIGN_HOST(1, "改派主办单位"),
    ADD_ASSIST(2, "增加协办单位"),
    REMOVE_ASSIST(3, "取消协办单位");

    public static final int[] ARRAYS = Arrays.stream(values()).mapToInt(ReassignTypeEnum::getType).toArray();

    /**
     * 类型值
     */
    private final Integer type;

    /**
     * 类型名称
     */
    private final String name;

    @Override
    public int[] array() {
        return ARRAYS;
    }

    public static ReassignTypeEnum valueOf(Integer type) {
        return Arrays.stream(values())
                .filter(e -> e.getType().equals(type))
                .findFirst()
                .orElse(null);
    }

    public static String getName(Integer type) {
        ReassignTypeEnum enumObj = valueOf(type);
        return enumObj != null ? enumObj.getName() : "";
    }
}
```

### 3.9 接访方式枚举 (ReceiveTypeEnum)

```java
package cn.iocoder.yudao.module.fz.enums.review;

import cn.iocoder.yudao.framework.common.core.IntArrayValuable;
import lombok.AllArgsConstructor;
import lombok.Getter;

import java.util.Arrays;

/**
 * 接访方式枚举
 */
@Getter
@AllArgsConstructor
public enum ReceiveTypeEnum implements IntArrayValuable {

    ON_SITE(1, "现场接访"),
    PHONE(2, "电话接访"),
    VIDEO(3, "视频接访");

    public static final int[] ARRAYS = Arrays.stream(values()).mapToInt(ReceiveTypeEnum::getType).toArray();

    /**
     * 类型值
     */
    private final Integer type;

    /**
     * 类型名称
     */
    private final String name;

    @Override
    public int[] array() {
        return ARRAYS;
    }

    public static ReceiveTypeEnum valueOf(Integer type) {
        return Arrays.stream(values())
                .filter(e -> e.getType().equals(type))
                .findFirst()
                .orElse(null);
    }

    public static String getName(Integer type) {
        ReceiveTypeEnum enumObj = valueOf(type);
        return enumObj != null ? enumObj.getName() : "";
    }
}
```

### 3.10 单位类型枚举 (UnitTypeEnum)

```java
package cn.iocoder.yudao.module.fz.enums.review;

import cn.iocoder.yudao.framework.common.core.IntArrayValuable;
import lombok.AllArgsConstructor;
import lombok.Getter;

import java.util.Arrays;

/**
 * 单位类型枚举
 */
@Getter
@AllArgsConstructor
public enum UnitTypeEnum implements IntArrayValuable {

    HOST(1, "主办单位"),
    ASSIST(2, "协办单位"),
    SUPERVISION(3, "督办单位");

    public static final int[] ARRAYS = Arrays.stream(values()).mapToInt(UnitTypeEnum::getType).toArray();

    /**
     * 类型值
     */
    private final Integer type;

    /**
     * 类型名称
     */
    private final String name;

    @Override
    public int[] array() {
        return ARRAYS;
    }

    public static UnitTypeEnum valueOf(Integer type) {
        return Arrays.stream(values())
                .filter(e -> e.getType().equals(type))
                .findFirst()
                .orElse(null);
    }

    public static String getName(Integer type) {
        UnitTypeEnum enumObj = valueOf(type);
        return enumObj != null ? enumObj.getName() : "";
    }
}
```

---

## 变更历史

| 版本 | 日期 | 变更内容 | 变更人 |
|-----|------|---------|--------|
| v1.0 | 2026-04-13 | 初始版本，完成DO实体类、VO类、枚举类设计 | Claude |